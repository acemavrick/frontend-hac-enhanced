# Drizzle
DATABASE_URL=file:local.db

# HAC Scraper API
SCRAPER_API_URL=http://localhost:5301

# encryption key for HAC passwords at rest (64-char hex)
# generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
